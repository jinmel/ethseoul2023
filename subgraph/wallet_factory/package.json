{
  "name": "wallet_factory",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wallet_factory",
    "create-local": "graph create --node http://localhost:8020/ wallet_factory",
    "remove-local": "graph remove --node http://localhost:8020/ wallet_factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wallet_factory",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
